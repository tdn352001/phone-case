import { Metadata } from 'next'
import RegisterPageContent from '~/modules/register'

const RegisterPage = () => {
  return <RegisterPageContent />
}

export const metadata: Metadata = {
  title: 'Login',
}

export default RegisterPage
